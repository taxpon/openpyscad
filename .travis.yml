language: python
sudo: required
dist: xenial
python:
  - 2.7
  - 3.4
  - 3.5
  - 3.6
  - 3.7
  - 3.8-dev

install:
  - pip install coveralls pep8 six

script:
  - bash pep8.sh
  - nosetests  --with-coverage --cover-package=openpyscad

after_success:
  - coveralls
